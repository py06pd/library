<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
    <head>
        <meta charset="UTF-8" />
        <title>Welcome!</title>      
        <link rel="stylesheet" href="{{ asset("css/base.css") }}" />
    </head>
    <body>
        <div id="app">
            <span id="data" data="{{ data }}"/>
            <div id="content">
                <div class="MenuContent">
                    <cic-menu></cic-menu>
                    <cic-login></cic-login>
                </div>
                <div class="PageContent">
                    <div v-if="page === 'myaccount'">
                        <cic-myaccount></cic-myaccount>
                    </div>
                    <div v-if="page === 'books'">
                        <cic-book-list></cic-book-list>
                    </div>
                    <div v-if="page === 'lending'">
                        <cic-lending-list></cic-lending-list>
                    </div>
                    <div v-if="page === 'users'">
                        <cic-user-list></cic-user-list>
                    </div>
                    <div v-if="page === 'wishlist'">
                        <cic-wishlist :ibooks="params.books" :iuserid="params.userid"></cic-wishlist>
                    </div>
                </div>
            </div>
        </div>
        <script type="text/javascript" src="{{ asset("js/main.js") }}"></script>
    </body>
</html>
